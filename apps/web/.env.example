# Internal API base URL (server-side only, never exposed to the browser)
API_URL=http://localhost:3001

# Public site metadata
NEXT_PUBLIC_SITE_NAME="BOX33"
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_DEFAULT_LOCALE=es

# Shared secret for the on-demand ISR revalidation hook (min 32 chars)
REVALIDATION_SECRET=
